#!/usr/bin/env bash
# Clean mesh and time directories for the CPU baseline case.
cd "${0%/*}" || exit 1

rm -rf [1-9]* 0.[0-9]* processor* postProcessing
rm -rf constant/polyMesh/{points,faces,owner,neighbour,boundary,cellZones,faceZones,pointZones,sets,*.gz}
rm -f log.*

echo "Cleaned laplaceCpu case."
